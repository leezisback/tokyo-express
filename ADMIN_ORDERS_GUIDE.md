# Руководство по работе с заказами в админ-панели

## Обзор

Админ-панель предоставляет полный контроль над заказами: просмотр, фильтрация, изменение статусов и редактирование деталей заказа.

## Доступ

URL: http://localhost:5173/admin/orders

**Требуется авторизация:**
- Логин: `admin`
- Пароль: `admin123`

## Основные возможности

### 1. Таблица заказов

#### Отображаемая информация

- **№ / Время** - короткий ID заказа и время создания
- **Клиент** - телефон клиента
- **Тип** - Доставка или Самовывоз
- **Состав / Адрес** - краткий список товаров, адрес доставки, комментарий
- **Сумма** - детализация: товары, доставка, итого
- **Статус** - текущий статус заказа
- **Действия** - кнопка "Открыть" для детального просмотра

#### Фильтрация

**По типу заказа:**
- Все
- Доставка
- Самовывоз

**По статусу:**
- Все
- Новый
- Принят
- Готовится
- Передан курьеру
- В пути
- Выполнен
- Отменен

### 2. Детальный просмотр заказа

При клике на заказ или кнопку "Открыть" открывается модальное окно с подробной информацией.

#### Заголовок

- Номер заказа
- Дата и время создания
- Тип получения (Доставка/Самовывоз)
- Кнопка закрытия (✕)

#### Управление заказом

**Изменение статуса:**
- Dropdown со всеми доступными статусами
- Изменение применяется немедленно при выборе

**Редактирование заказа:**
1. Нажмите кнопку "Редактировать"
2. Откроется режим редактирования:
   - Телефон (редактируемое поле)
   - Адрес доставки (если доставка):
     - Улица
     - Дом
     - Квартира
     - Подъезд
     - Этаж
     - Частный дом (чекбокс)
   - Комментарий (текстовое поле)
3. Нажмите "Сохранить" или "Отмена"

#### Информационные блоки

**Адрес доставки** (для доставки):
- Улица, дом
- Подъезд, этаж, квартира
- Отметка "частный дом"

**Комментарий клиента:**
- Любые дополнительные пожелания или инструкции

**Стоимость:**
- Товары (subtotal)
- Доставка
- Итого (total)

### 3. Состав заказа

Отображается список всех товаров в заказе с детальной информацией.

#### Информация о каждом товаре

**Визуальное представление:**
- 📷 **Изображение товара** (64×64px, квадрат с закругленными углами)
- **Название товара**
- **Вес** (если указан)
- **Состав** (ингредиенты, отображается до 2 строк)

**Ценообразование:**
- **Итоговая сумма** за позицию (крупно)
- **Детализация**: цена × количество (мелко)

Пример:
```
395 ₽
245 ₽ × 1 шт
```

#### Особенности отображения

- Изображения загружаются через систему `getImageUrl()` для корректного отображения
- Если изображение отсутствует, показывается плейсхолдер "фото"
- Состав ограничен 2 строками с многоточием
- Все позиции разделены горизонтальными линиями

## API-интеграция

### Backend

**Endpoint:** `GET /api/orders`

**Требования:**
- Bearer token в заголовке `Authorization`
- Роль: `admin`

**Возвращает:**
```json
[
  {
    "_id": "...",
    "shortId": "ABC123",
    "items": [
      {
        "product": {
          "_id": "...",
          "name": "Запечённая филадельфия",
          "image": "http://localhost:5001/uploads/card-1.png",
          "price": 695,
          "weight": "245 г",
          "composition": "рис, нори, сливочный сыр..."
        },
        "name": "Запечённая филадельфия",
        "price": 695,
        "qty": 1
      }
    ],
    "subtotal": 695,
    "deliveryPrice": 150,
    "total": 845,
    "mode": "delivery",
    "address": { ... },
    "phone": "+79001234567",
    "comment": "Позвонить за 10 минут",
    "status": "new",
    "createdAt": "2024-12-25T10:30:00.000Z"
  }
]
```

**Populate:**
Backend использует `.populate()` для получения полной информации о товарах:

```javascript
.populate({
    path: "items.product",
    select: "name image price weight composition description",
})
```

### Frontend

**Компонент:** `front/src/pages/Admin/Orders.jsx`

**Функции:**
- `adminFetchOrders()` - получение списка заказов
- `adminUpdateOrderStatus()` - изменение статуса
- `adminUpdateOrder()` - редактирование заказа
- `normalizeOrderItem()` - нормализация данных товара для отображения
- `getImageUrl()` - преобразование путей к изображениям

## Статусы заказов

| Статус | Значение | Описание |
|--------|----------|----------|
| `new` | Новый | Только что созданный заказ |
| `accepted` | Принят | Заказ принят в обработку |
| `cooking` | Готовится | Заказ готовится |
| `to_courier` | Передан курьеру | Заказ передан курьеру |
| `on_way` | В пути | Курьер в пути |
| `done` | Выполнен | Заказ выполнен |
| `canceled` | Отменен | Заказ отменен |

## UI/UX особенности

### Таблица заказов

- Клик по всей строке открывает детальный просмотр
- Hover эффект для улучшения интерактивности
- Адаптивная сетка с выравниванием по типу данных
- Эмодзи для визуального разделения информации (📍 адрес, 💬 комментарий)

### Модальное окно

- Закрывается по клику на оверлей
- Закрывается по кнопке ✕
- Закрывается по клавише Escape
- Скролл внутри окна при большом количестве товаров
- Максимальная высота: 90vh

### Режим редактирования

- Визуальное разделение режимов просмотра и редактирования
- Кнопки "Сохранить" и "Отмена" для явного управления
- Disable статуса во время редактирования
- Индикация загрузки при сохранении

### Состав заказа

- Карточный дизайн вместо таблицы
- Крупные изображения для лучшей визуализации
- Выделение итоговой суммы
- Деликатная детализация цены

## Типичные сценарии использования

### Прием нового заказа

1. Заказ появляется в таблице со статусом "Новый"
2. Администратор открывает заказ
3. Проверяет состав, адрес, комментарий
4. Изменяет статус на "Принят"

### Изменение адреса доставки

1. Администратор открывает заказ
2. Нажимает "Редактировать"
3. Корректирует адрес (например, уточняет квартиру)
4. Нажимает "Сохранить"
5. Изменения применяются к заказу

### Отмена заказа

1. Администратор открывает заказ
2. Выбирает статус "Отменен" из dropdown
3. Заказ помечается как отменен

### Поиск конкретного заказа

1. Используйте фильтры по типу и статусу
2. Просматривайте краткий состав прямо в таблице
3. Сортировка по дате (новые сверху)

## Отладка

### Позиции не отображаются

**Debug-блок:**
Если состав пуст, но `selected.items` не пустой, отображается JSON первого элемента для диагностики формата данных.

**Проверьте:**
1. Backend делает populate для `items.product`
2. Frontend получает данные через API
3. Функция `normalizeOrderItem()` корректно обрабатывает разные форматы

### Изображения не загружаются

**Проверьте:**
1. Backend раздаёт статику `/uploads`
2. `getImageUrl()` корректно преобразует пути
3. Изображения существуют в `backApi/uploads/`
4. Переменная `VITE_API_URL` настроена в `front/.env`

## Производительность

- Заказы загружаются один раз при монтировании компонента
- Обновление статуса происходит оптимистично (UI обновляется сразу)
- Изображения загружаются с `loading="lazy"` (хотя в модалке это не критично)
- Используется `useMemo` для фильтрации и нормализации данных

## Безопасность

- Все операции требуют JWT токен
- Роль `admin` проверяется на backend
- Валидация входных данных при редактировании
- Санитизация телефона и адреса

## Будущие улучшения

- Пагинация для большого количества заказов
- Экспорт заказов в CSV/Excel
- Push-уведомления о новых заказах
- История изменений заказа
- Печать чека/накладной
- Поиск по номеру телефона или ID
