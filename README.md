# Tokyo Express - Сайт доставки суши и роллов

Веб-приложение для онлайн-заказа японской кухни с административной панелью.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Backend
cd backApi
npm install

# Frontend
cd ../front
npm install
```

### 2. Настройка переменных окружения

**Backend (`backApi/.env`):**
```env
MONGO_URI=mongodb://localhost:27017/tokyo-express
JWT_SECRET=your-secret-key-here
CLIENT_URL=http://localhost:5173
PORT=5001
```

**Frontend (`front/.env`):**
```env
VITE_API_URL=http://localhost:5001
```

### 3. Запуск MongoDB

```bash
brew services start mongodb-community
```

### 4. Заполнение тестовыми данными

```bash
cd backApi
npm run seed
```

Будут созданы:
- 8 категорий товаров
- 3 товара с изображениями
- Админ: логин `admin`, пароль `admin123`
- Тестовая акция

### 5. Запуск приложения

**Backend:**
```bash
cd backApi
npm run dev
# Запустится на http://localhost:5001
```

**Frontend:**
```bash
cd front
npm run dev
# Запустится на http://localhost:5173
```

## 📁 Структура проекта

```
tokyo-express/
├── backApi/              # Backend (Node.js + Express)
│   ├── src/
│   │   ├── models/      # Mongoose модели
│   │   ├── routes/      # API маршруты
│   │   ├── controllers/ # Контроллеры
│   │   ├── middlewares/ # Middleware (auth, errors)
│   │   └── seed/        # Seed скрипты
│   └── uploads/         # Загруженные изображения
│
├── front/               # Frontend (React + Vite)
│   ├── src/
│   │   ├── pages/      # Страницы
│   │   ├── components/ # Компоненты
│   │   ├── lib/        # Утилиты, API
│   │   └── context/    # React Context
│   └── public/         # Статические файлы
│
└── docs/               # Документация
    ├── IMAGES_SETUP.md
    └── SEED_INSTRUCTIONS.md
```

## 🛠️ Основные функции

### Публичная часть

- ✅ Главная страница с слайдером акций
- ✅ Каталог товаров с фильтрацией и сортировкой
- ✅ Карточки товаров с изображениями, составом, ценами
- ✅ Отображение акционных товаров со старой ценой
- ✅ Корзина с изменением количества
- ✅ Оформление заказа (доставка/самовывоз)
- ✅ Страница контактов

### Административная панель

- ✅ Авторизация админа
- ✅ Управление категориями (CRUD)
- ✅ Управление товарами (CRUD)
- ✅ Загрузка изображений товаров
- ✅ Управление акциями
- ✅ Список заказов с фильтрами
- ✅ Детальный просмотр заказа
- ✅ Смена статусов заказов
- ✅ Редактирование заказов (адрес, телефон, комментарий)

## 🔑 Доступ к админ-панели

URL: http://localhost:5173/admin/login

- **Логин:** `admin`
- **Пароль:** `admin123`

## 📸 Загрузка изображений

### Через админ-панель

1. Войдите в админ-панель
2. Перейдите в "Меню"
3. Создайте или отредактируйте товар
4. Нажмите "Выбрать файл" в поле "Фото товара"
5. Изображение автоматически загрузится на сервер
6. Превью отобразится справа

### Технические детали

- **Максимальный размер:** 5 МБ
- **Форматы:** Любые изображения (image/*)
- **Хранение:** `backApi/uploads/`
- **Раздача:** `http://localhost:5001/uploads/filename.jpg`

Подробнее: [IMAGES_SETUP.md](./IMAGES_SETUP.md)

## 🗃️ База данных

### Модели

- **Category** - категории товаров
- **Product** - товары
- **Promotion** - акции
- **Order** - заказы
- **User** - пользователи (администраторы)

### Пересоздание БД

```bash
cd backApi
npm run seed
```

## 🧪 API Endpoints

### Публичные

- `GET /api/catalog/categories` - список категорий
- `GET /api/catalog/products` - список товаров
- `POST /api/orders` - создание заказа
- `GET /api/promotions` - активные акции

### Админские (требуют Bearer token)

- `POST /api/auth/login` - авторизация
- `POST /api/catalog/categories` - создание категории
- `PUT /api/catalog/categories/:id` - редактирование категории
- `DELETE /api/catalog/categories/:id` - удаление категории
- `POST /api/catalog/products` - создание товара
- `PUT /api/catalog/products/:id` - редактирование товара
- `DELETE /api/catalog/products/:id` - удаление товара
- `POST /api/upload/image` - загрузка изображения
- `GET /api/orders` - список заказов
- `PATCH /api/orders/:id/status` - изменение статуса заказа
- `PATCH /api/orders/:id` - редактирование заказа

## 🐛 Устранение проблем

### Изображения не отображаются

1. Проверьте, что папка `backApi/uploads/` содержит файлы
2. Убедитесь, что в `front/.env` указан `VITE_API_URL=http://localhost:5001`
3. Перезапустите frontend после изменения `.env`

### Ошибка подключения к MongoDB

```bash
# Проверьте статус
brew services list | grep mongodb

# Запустите MongoDB
brew services start mongodb-community
```

### Backend не запускается

1. Проверьте `.env` файл в `backApi/`
2. Убедитесь, что порт 5001 свободен
3. Установите зависимости: `npm install`

### Frontend не запускается

1. Проверьте `.env` файл в `front/`
2. Убедитесь, что порт 5173 свободен
3. Установите зависимости: `npm install`

## 📚 Дополнительная документация

- [Инструкция по работе с изображениями](./IMAGES_SETUP.md)
- [Инструкция по работе с Seed](./SEED_INSTRUCTIONS.md)

## 🛠️ Технологический стек

### Backend

- Node.js + Express
- MongoDB + Mongoose
- JWT для авторизации
- Multer для загрузки файлов
- bcrypt для хеширования паролей

### Frontend

- React 18
- Vite
- React Router v6
- TailwindCSS
- Context API для состояния

## 📝 Требования к системе

- Node.js 18+
- MongoDB 6+
- npm или yarn

## 🤝 Разработка

```bash
# Backend в dev режиме
cd backApi
npm run dev

# Frontend в dev режиме
cd front
npm run dev
```

## 📦 Сборка для продакшена

```bash
# Frontend
cd front
npm run build
# Результат в front/dist/

# Backend запускается как есть
cd backApi
npm start
```

## 📄 Лицензия

MIT
